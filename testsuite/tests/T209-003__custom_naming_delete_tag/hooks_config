[hooks]
        from-domain = example.com
        mailinglist = repo@example.com
        allow-delete-tag = true

        # Allow tags in custom namespaces
        tag-ref-namespace = refs/vendor/.*/tags/.*
        tag-ref-namespace = refs/user/.*/tags/.*
