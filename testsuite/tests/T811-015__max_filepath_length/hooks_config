[hooks]
        from-domain = adacore.com
        mailinglist = git-hooks-ci@example.com
        filer-email = filer@example.com

        # Reduce the maximum file path length to something that can
        # be more easily tested.
        max-filepath-length = 12
